[project]
name = "roost-palette"
version = "0.1.0"
description = ""
requires-python = ">=3.14"              # floor = pin (spec §5); promotion lowers this to the matrix floor
dependencies = []

[project.scripts]
roost-palette = "roost_palette.cli:main"

[dependency-groups]
dev = ["pytest>=8", "ruff"]

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
